# Shared package — design-spec

This package is the synchronization layer for web and app targets.

Keep these things here:

- product app name and slug
- screen IDs, routes, and reference mappings
- shared API contracts and mock data models
- token snapshots exported from the UI explorer
- platform-neutral helpers

Avoid putting web-only JSX, React Native views, Flutter widgets, or platform-specific storage code here.
