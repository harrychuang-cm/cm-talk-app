# Reference screenshots

Screenshots are stored once in `../design-reference/` to avoid duplicating image binaries in this ZIP.
Use paths like `design-reference/screen-0.png` in prompts and visual parity checks.
